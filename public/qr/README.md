# Corridor zone QR-codes

Gegenereerd met `npm run generate:qr` (`scripts/generate-zone-qrs.mjs`).

| Bestand | URL | Kleur |
|---------|-----|-------|
| `zones.png` / `.svg` | https://corridor.gent/zones | zwart |
| `zone-1` … `zone-10`, `zone-12` | detail-URL | zone-omslag accent |
| `zone-11` (Corri Arts) | https://corridor.gent/zones/11 | pastel-regenboog |

Zie `zone-colors.json`. Print-sheet: `/beheer/qr`.
